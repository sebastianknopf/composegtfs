{
  "distance_influence": 50,
  "priority": [
    { "if": "road_access == PRIVATE",     "multiply_by": "0"   },
    { "if": "road_access == DESTINATION", "multiply_by": "0.3" }
  ],
  "speed": [
    { "if": "true", "limit_to": "car_average_speed" }
  ]
}
